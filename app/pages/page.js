// pages/index.js
import Layout from "@/components/layout";

const Home = () => {
  return (
    <Layout>
      <div>
      <h2>API Dashboard</h2>
      </div>
      <div>Use the Navigation to Display DataTables per each application, review the raw JSON Data, Filter, Sort and Query the API Data</div>
    </Layout>
  );
};

export default Home;
